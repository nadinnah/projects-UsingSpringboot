spring.application.name=taco-cloud
spring.thymeleaf.cache=true

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver

spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

#Always run schema.sql and data.sql at application startup
##spring.sql.init.mode=always

#WAIT until Hibernate finishes creating tables
#THEN run data.sql
#as the order before was (wrong order):
#data.sql runs
#Hibernate creates tables
##spring.jpa.defer-datasource-initialization=true


taco.orders.page-size=10
spring.data.rest.base-path=/api
